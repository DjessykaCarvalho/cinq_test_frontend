import React from 'react';

const UserItem = ({location}) => {
  console.log(location);
  
  return(
    <div>
      <div>{data.firstName} {data.lastName}</div>


    </div>
  )
}

export default UserItem;